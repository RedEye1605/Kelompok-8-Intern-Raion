import 'package:flutter/material.dart';

class TransportasiTab extends StatelessWidget {
  const TransportasiTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Transportasi")],
      ),
    );
  }
}